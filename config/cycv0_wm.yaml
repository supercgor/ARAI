defaults:
  - _self_
  - model: cycv0
  - setting: default
  - dataset: cycv0_wm
  - criterion: unet

test_path: ${data.test_path}
checkpoint: ${model.checkpoint}
label: False
transform: False
output: ~
